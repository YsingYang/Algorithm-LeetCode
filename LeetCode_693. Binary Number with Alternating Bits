class Solution {
    /********
    
    用异或可以轻松解决这个问题
    
    ********/
public:
    bool hasAlternatingBits(int n) {
        while(n >= 2) {
            if((n & 1) ^ (n & 2) == 0){
                return false;
            }
            n >>= 1;
        }
        return true;
    }
};
